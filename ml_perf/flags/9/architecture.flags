# architecture.flags: Flags that control the model architecture.

--conv_width=64
--fc_width=64
--trunk_layers=9
--value_cost_weight=0.5
--summary_steps=64

--bool_features=1

# --input_features=$MODEL must match --model=random:$MODEL in bootstrap.flags.
--input_features=mlperf07
