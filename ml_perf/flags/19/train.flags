# train.flags: Flags for training.

--flagfile=ml_perf/flags/19/architecture.flags

--shuffle_buffer_size=0
--shuffle_examples=false

# see --train_filter in rl_loop.flags
# --filter_amount=1

# Device specific hyperparameters re: batch size and LR schedules.
--train_batch_size=4096

--lr_rates=0.016
--lr_rates=0.16
--lr_rates=0.016
--lr_rates=0.0016

--lr_boundaries=128
--lr_boundaries=10000
--lr_boundaries=20000
--l2_strength=0.0001
