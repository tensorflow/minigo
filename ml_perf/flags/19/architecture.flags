# architecture.flags: Flags that control the model architecture.

--conv_width=64
--fc_width=64
--trunk_layers=6
--value_cost_weight=0.5
--summary_steps=128

--bool_features=1

# --input_features=$FEATURES and --input_layout=$LAYOUT must match
# --model=random:$FEATURES:$LAYOUT:0 in bootstrap.flags.
--input_features=mlperf07
--input_layout=nchw
